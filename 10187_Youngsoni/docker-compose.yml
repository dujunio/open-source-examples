#Youngsoni 1.0.0

version: '2.4'

services:
  data:
    image: dujunio/youngsoni
    volumes:
      - wwwroot:/wwwroot
      - vhost:/etc/nginx/sites-enabled
    tty: true

  nginx:
    image: dujunio/nginx
    depends_on:
      - data
    volumes:
      - wwwroot:/wwwroot
      - vhost:/etc/nginx/sites-enabled
    ports:
      - "10187:80"
    tty: true
    container_name: youngsoni

volumes:
  wwwroot:
  vhost:
