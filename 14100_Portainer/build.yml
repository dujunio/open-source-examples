version: '2.4'

services:
  docker:
    build:
      context: ../00000_BASE/04000_docker
      dockerfile: Dockerfile
    image: dujunio/docker

  portainer:
    build:
      context: ./
      dockerfile: Dockerfile
    image: dujunio/portainer
