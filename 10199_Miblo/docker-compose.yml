#Miblo

version: '2.4'

services:
  data:
    image: dujunio/miblo
    volumes:
      - wwwroot:/wwwroot
      - vhost:/etc/nginx/sites-enabled
    tty: true

  nginx:
    image: dujunio/nginx
    depends_on:
      - data
    volumes:
      - wwwroot:/wwwroot
      - vhost:/etc/nginx/sites-enabled
    ports:
      - "10199:80"
    tty: true
    container_name: miblo

volumes:
  wwwroot:
  vhost:
