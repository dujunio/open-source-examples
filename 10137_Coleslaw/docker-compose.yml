#Coleslaw

version: '2.4'

services:
  data:
    image: dujunio/coleslaw
    volumes:
      - wwwroot:/wwwroot
      - vhost:/etc/nginx/sites-enabled
    tty: true

  nginx:
    image: dujunio/nginx
    depends_on:
      - data
    volumes:
      - wwwroot:/wwwroot
      - vhost:/etc/nginx/sites-enabled
    ports:
      - "10137:80"
    tty: true
    container_name: coleslaw

volumes:
  wwwroot:
  vhost:
